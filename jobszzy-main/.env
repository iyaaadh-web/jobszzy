# Server Environment Variables
PORT=5000
JWT_SECRET=jobszzy_prod_secret_9921
NODE_ENV=production

# Frontend Environment Variables (for Vite)
# Using relative path '/api' so it works via Nginx proxy
VITE_API_BASE_URL=/api

# SMTP Email Settings (Required for Forgot Password)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=support@jobszzy.com
SMTP_PASS=Idhu@0412
SMTP_FROM=support@jobszzy.com
